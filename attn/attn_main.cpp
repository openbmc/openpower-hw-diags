#include <attn_handler.hpp>

/**
 * @brief Attention handler application main()
 *
 * This is the main interface to the Attention handler application.
 */
int main()
{
    // TEMP CODE - begin
    // In a future version  this will setup the attention gpio
    // monitor and  attnHandler() will be executed in response
    // to a gpio event.

    attnHandler(); // handle active attentions

    // TEMP CODE - end

    return 0;
}
